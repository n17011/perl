#!/home/n17011/perl

use warnings;
use strict;

my @msg = qw/abc def 123/;
def/

for (my $var = 0; $var < @msg; $var++){
	if($msg[$var] = ~/ab/){
		print "$msg[$var]あたってる";
	}else{print "$msg[$var]ちがう"}
}
